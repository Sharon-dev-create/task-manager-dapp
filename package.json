{
  "name": "task-manager-contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "cd frontend && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:ignition": "hardhat ignition deploy ignition/modules/TaskManagerDeployment.js --network sepolia",
    "verify": "hardhat verify --network sepolia"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@nomicfoundation/hardhat-ignition": "^0.15.0",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
